rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // TEMP DEBUG MODE: abrir acesso para diagnosticar upload/CORS em producao.
      // IMPORTANTE: reverter para regras com autenticacao apos validacao.
      allow read, write: if true;
    }
  }
}